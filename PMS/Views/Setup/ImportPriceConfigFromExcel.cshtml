@{
    ViewBag.Header = "PriceConfig";
    ViewBag.HeaderDescription = "Import from excel";
    ViewBag.Title = "Import from excel | PMS";
}

@section styles{
    <!-- DataTables -->
    <link rel="stylesheet" href="~/Assets/bower_components/datatables.net-bs/css/dataTables.bootstrap.min.css">
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title"></h3>
    </div>
    <div class="box box-danger">
        <div class="box-header">
            <h3 class="box-title"></h3>
        </div>
        <div class="box-body">
            @if (ViewBag.error != null)
            {
                <div class="alert alert-danger alert-dismissible">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                    <h4><i class="icon fa fa-ban"></i> Alert!</h4>
                    @ViewBag.error
                </div>
            }@if (ViewBag.success != null)
            {
                <div class="alert alert-success alert-dismissible">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                    <h4><i class="icon fa fa-check"></i> Alert!</h4>
                    @ViewBag.success
                </div>
            }


            <div class="col-sm-12 " style="margin-bottom:30px ; padding:0px;">
                <div class="input-group-btn">
                    <a href="@Url.Action("PriceConfig", "Setup")" class="btn btn-themyriad"><i class="fa fa-plus" style="padding-right: 5px;"></i> All PriceConfig List</a>
                </div>
            </div>

            <div style="overflow: auto; width:100%">

                <table id="priceConfig-table" class="table table-bordered table-striped">

                    <thead>

                        <tr>
                            <th>Status</th>
                            <th>Term</th>
                            <th>Room Type</th>
                            <th>Price</th>
                            <th>Deposit</th>
                            <th>Available</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var price in ViewBag.notSavedPriceConfig as List<PMS.DTO.ViewModels.SetupViewModels.AddPriceConfigVM>)
                        {
                            <tr>
                                <td><span class='label label-danger'>Failed</span></td>
                                <td>@price.TermName </td>
                                <td>@price.RoomName</td>
                                <td>@(price.Currency + " " + String.Format("{0:f3}", price.Price))</td>
                                <td>@(price.Currency + " " + String.Format("{0:f3}", price.Deposit))</td>
                                <td>
                                    @if (price.IsAvailable == true)
                                    {
                                        <span class="label label-success">Yes</span>
                                    }
                                    else
                                    {
                                        <span class="label label-danger">No</span>
                                    }

                                </td>
                            </tr>
                        }

                        @foreach (var price in ViewBag.savedPriceConfig as List<PMS.DTO.ViewModels.SetupViewModels.AddPriceConfigVM>)
                        {
                            <tr>
                                <td><span class='label label-success'>Success</span></td>
                                <td>@price.TermName</td>
                                <td>@price.RoomName</td>
                                <td>@(price.Currency + " " + String.Format("{0:f3}", price.Price))</td>
                                <td>@(price.Currency + " " + String.Format("{0:f3}", price.Deposit))</td>
                                <td>
                                    @if (price.IsAvailable == true)
                                    {
                                        <span class="label label-success">Yes</span>
                                    }
                                    else
                                    {
                                        <span class="label label-danger">No</span>
                                    }

                                </td>
                            </tr>
                        }

                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Status</th>
                            <th>Term</th>
                            <th>Room Type</th>
                            <th>Price</th>
                            <th>Deposit</th>
                            <th>Available</th>
                        </tr>
                    </tfoot>
                </table>

            </div>

        </div>
    </div>
</div>


@section scripts{

    <script src="~/Assets/bower_components/datatables.net/js/jquery.dataTables.min.js"></script>
    <script src="~/Assets/bower_components/datatables.net-bs/js/dataTables.bootstrap.min.js"></script>
    <script src="//cdn.datatables.net/plug-ins/1.10.11/sorting/date-eu.js" type="text/javascript"></script>

    <script>
        $(function () {
            $('#priceConfig-table').DataTable({});
        })

    </script>
}
