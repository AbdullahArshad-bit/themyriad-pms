@using PMS.Common.Classes

@{
    ViewBag.Header = "Rating List";
    ViewBag.HeaderDescription = "View Ratings";
    ViewBag.Title = "View Ratings | PMS";

    var ratingItems = ViewBag.viewRatings as List<PMS.DTO.ViewModels.InspectionViewModels.RatingListItem>;
}

@section styles{
    <!-- DataTables -->
    <link rel="stylesheet" href="~/Assets/bower_components/datatables.net-bs/css/dataTables.bootstrap.min.css">
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title"></h3>
    </div>
    <div class="box box-danger">
        <div class="box-header">
            <h3 class="box-title"></h3>
        </div>
        <div class="box-body">
            @if (ViewBag.error != null)
            {
                <div class="alert alert-danger alert-dismissible">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                    <h4><i class="icon fa fa-ban"></i> Alert!</h4>
                    @ViewBag.error
                </div>
            }@if (ViewBag.success != null)
            {
                <div class="alert alert-success alert-dismissible">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                    <h4><i class="icon fa fa-check"></i> Alert!</h4>
                    @ViewBag.success
                </div>
            }


            <div class="col-sm-12 " style="margin-bottom:30px ; padding:0px;">
                <div class="input-group-btn">
                    <a href="@Url.Action("RatingList", "Inspection")" class="btn btn-themyriad"><i class="fa fa-arrow-left" style="padding-right: 5px;"></i>All Rating List</a>
                </div>
            </div>

            <div style="overflow: auto; width:100%;white-space:nowrap;">

                <table class="table table-bordered table-striped data-table">

                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Description</th>
                            <th>Percent of Charge</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in ratingItems)
                        {
                            <tr>
                                <td>@item.RatingListItemName</td>
                                <td>@item.RatingListItemDescripttion</td>
                                <td>@item.RatingListItemPercent</td>

                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Name</th>
                            <th>Description</th>
                            <th>Percent of Charge</th>
                        </tr>
                    </tfoot>
                </table>

            </div>

        </div>
    </div>
</div>

@section scripts{

    <script src="~/Assets/bower_components/datatables.net/js/jquery.dataTables.min.js"></script>
    <script src="~/Assets/bower_components/datatables.net-bs/js/dataTables.bootstrap.min.js"></script>
    <script src="//cdn.datatables.net/plug-ins/1.10.11/sorting/date-eu.js" type="text/javascript"></script>

    <script>
        $(function () {
            $('.data-table').DataTable({});
        })


    </script>
}
